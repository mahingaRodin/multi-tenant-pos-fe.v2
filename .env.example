# Local Vite → Spring Boot on this machine
VITE_API_BASE_URL=http://localhost:5000

# Production on Vercel: do not set VITE_API_BASE_URL (or set it to empty).
# The app calls /api/* on the same origin; vercel.json proxies to Azure.
